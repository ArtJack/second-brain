# Synthetic fixture: administrative preferences

This file is regression-test data, not a learned user memory.

Invoices should be delivered as PDF files with visible line items. Engineering updates
should be short implementation summaries that include exact file paths. Destructive
actions require confirmation before execution.
